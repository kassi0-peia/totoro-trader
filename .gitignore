node_modules/
dist/

# mkcert TLS material — never commit private keys
server/certs/
*.pem

# runtime state
server/.basis-cache.json
server/.trades.json
server/.journal.json
server/.journal-shots/

# machine-local Claude Code settings
.claude/settings.local.json

# generated basis-check reports
basis-check-*.txt

# recorded demo clips (REC button)
*.webm

# original mascot source (the transparent cut-out lives at src/totoro.png)
totoro-original.png

# desktop screenshots dropped in the repo folder
Screenshot from *.png

# session recaps stay local (our little secret)
RECAP-*.md

# Claude project instructions stay local (internal working doc)
CLAUDE.md

# process scratch dropped in the repo folder — notes, shared images, zips
attachments.zip
*.webp
*.jpeg
signal-*
totoro.png
totoro-build-steps.txt
totoro-review-tasks.md

# personal launch helper (hardcodes ~/ibc paths)
start-all.sh
.claude/
